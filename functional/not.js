
"use strict";
/*
   var not = fn => (...args) => !fn(...args); not(x=>x>5)(4);

  let not = fn => (args) => !fn(args);

 console.log( not(x=>x>5)(4)  );


  var x = x=> (x) => (x(x)) => (x(x))x => x(x(x)(x)) */

   var func = f => g(f) => g;

