
          var foopacks = [

           {
            unid : "007",
            name : "Foo Bar",
            desc : "The orginal Foo Bar"
           },

           {
            unid : "148",
            name : "Foo Bar Choclate",
            desc : "Your very favorite Foo Bar now in choclate"
           },

           {
            unid : "085",
            name : "Foo Bar Diet",
            desc : "The Zero-calorie Foo Bar"
           },

           {
            unid : "963",
            name : "Foo Bar Extra Foo",
            desc : "More foo for your Foo Bar"
           }

          ];
/*
function getFooById(foos,id) {

var name, desc;
 var i=1;
 var l=foos.length;
 var f=foos[0];
for ( ; i < l; i++) {
if( f && f.unid == id ) {
return {name : f.name, desc : f.desc} }
f=(!(foos[i]))?(return 0):f=foos[i];
}

}

console.log(getFooById(foopacks,"963"));
*/

var i=0;

for(; i<0; i++) {
console.log ( i + 'running' + (i+1) + 'time')
}

for(i=0;i<0;) {
console.log('x')
}

